#!/usr/bin/env ruby

require 'rubygems'
require 'officer'

options = {
  :dir_mode   => :system,
  :multiple   => false,
  :monitor    => true
}


Daemons.run_proc('officer', options) do
  Officer::Server.new.run
end